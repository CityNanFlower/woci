name: wo_ci
description: "蜗词 - 六级冲刺生词本"
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.0
  path: ^1.9.1
  shared_preferences: ^2.3.0
  flutter_local_notifications: ^19.0.0
  timezone: ^0.10.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/dict_cet6.json
